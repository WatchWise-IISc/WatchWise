# --- macOS ---
.DS_Store

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.ipynb_checkpoints/
.venv/
venv/
env/

# --- Node / frontend (regenerable: npm install / npm run build) ---
node_modules/
app/frontend/dist/

# --- Local tooling / IDE ---
.claude/
.idea/
.vscode/

# --- Secrets / environment ---
.env
*.env
*.key

# --- Canonical spec (excluded from the repo per request) ---
WATCHWISE_MASTER_SPEC.md

# --- Report materials (kept local until explicitly released) ---
REPORT.md
report/

# --- Large downloaded / cached / generated data (regenerable by the pipeline) ---
data/raw/
data/cache/
kaggle downloads/

# --- Cache tarballs built for the GitHub Release (uploaded as assets, not committed) ---
*-cache.tar.gz
